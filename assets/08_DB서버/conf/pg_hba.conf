# =============================================================
# DB 서버 — PostgreSQL 클라이언트 인증 설정
#
# [VULN-DB-02] INT 전체 서브넷(192.168.100.0/24)에서
# 모든 DB, 모든 사용자 접근 허용
#
# 올바른 설정 (참고):
#   host  agency_db    app_service  192.168.100.10/32  scram-sha-256
#   host  complaint_db app_service  192.168.100.12/32  scram-sha-256
# =============================================================

# TYPE  DATABASE        USER            ADDRESS                 METHOD

# 로컬 접속 (소켓)
local   all             postgres                                peer
local   all             all                                     peer

# IPv4 로컬
host    all             all             127.0.0.1/32            scram-sha-256

# [VULN-DB-02] INT 전체 서브넷에서 모든 DB, 모든 사용자 접근 허용
host    all             all             192.168.100.0/24        md5

# DMZ 외부 포털 서버 (OPNSense-2 경유)
host    mois_portal     app_service     203.238.140.10/32       md5

# IPv6 로컬
host    all             all             ::1/128                 scram-sha-256
