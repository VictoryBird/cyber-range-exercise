# ============================================================
# Dovecot 메인 설정 — 웹메일 서버 (192.168.100.11)
# ============================================================

protocols = imap lmtp
listen = *, ::
mail_debug = no
auth_debug = no
# [참고] 운영 중 디버그 로그 비활성 (블루팀 분석 시 활성화 가능)

!include conf.d/*.conf
!include_try local.conf
