# ============================================================
# Dovecot LMTP 배달 설정
# ============================================================

protocol lmtp {
  mail_plugins = $mail_plugins
  postmaster_address = postmaster@mois.local
}
